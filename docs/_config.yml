# GitHub Pages builds this folder (repo Settings → Pages → main, /docs).
# The site exists for one reason: public Privacy / Terms / account-deletion
# URLs that Google Play and the OAuth consent screen can link to.
title: FoxyCo
description: The gig driver's offer analyzer.

# Google Search Console ownership of https://cuproud.github.io/foxyco/, needed
# only if we ever chase OAuth brand verification (github.io is GitHub's domain,
# so the DNS-TXT route is impossible — this is the HTML-tag route). Emitted
# into <head> by jekyll-seo-tag.
google_site_verification: "-DYgRJY2SGihE1kqKR7ixMDIQ00EYRKKoULdSSYuRY8"
