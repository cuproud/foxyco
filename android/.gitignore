gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config: per-project, per-environment, and regenerated whenever a
# signing fingerprint is added (docs/FIREBASE_SETUP.md). The Android build fails
# without it — that is intended, not a bug to work around.
app/google-services.json
